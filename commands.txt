git clone https://github.com/glowang1/qtm350-quiz01.git cd qtm350-quiz01 touch commands.txt git add commands.txt git commit -m Create commands.txt to track commands git checkout -b feature-update mkdir reports cd reports touch summary.md echo This document provides a summary of the project updates. >> summary.md git add summary.md git commit -m Add reports directory with summary.md touch file{1..3}.txt git add file1.txt file2.txt file3.txt git commit -m Add empty files file1, file2, file3 to reports mv ../data/raw-data.csv ../data/input-data.csv git add ../data/input-data.csv git commit -m Rename raw-data.csv to input-data.csv mkdir -p ../scripts/backup && cp ../scripts/*.py ../scripts/backup/ git add ../scripts/backup git commit -m Bac kup all .py scripts into scripts/backup rm ../docs/documentation.md git add -A git c ommit -m Delete docs/documentation.md touch ../.gitignore echo temp/ >> ../.gitig nore echo dataset0?.csv >> ../.gitignore git add ../.gitignore git commit -m Add . gitignore file git checkout main git merge feature-update git add commands.txt git c ommit -m Update commands.txt with all commands used so far git push origin main
