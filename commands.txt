git clone https://github.com/glowang1/qtm350-quiz01.git 
cd qtm350-quiz01 
touch commands.txt 
git add commands.txt git commit -m Create commands.txt to track commands 
git checkout -b feature-update 
mkdir reports 
cd reports 
touch summary.md 
echo "This document provides a summary of the project updates." >> summary.md 
git add summary.md 
git add ~/qtm350-quiz01/reports
git commit -m "Add reports directory with summary.md" 
touch file{1..3}.txt && git add file1.txt && git add file2.txt && git add file3.txt 
mv ../data/raw-data.csv ../data/input-data.csv 
git add ../data/input-data.csv 
mkdir -p ../scripts/backup && cp ../scripts/*.py ../scripts/backup/ && git add ../scripts/backup 
rm ../docs/documentation.md 
git commit -m Update project files
touch .gitignore && git add .gitignore
echo temp/dataset0?.csv >> .gitignore
cat .gitignore
commit -m "add .gitignore file"
git switch main
git merge feature-update
git commit -m "all commands used"
git push origin main
